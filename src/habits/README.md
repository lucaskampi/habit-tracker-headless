Habits module: contains controller and service for creating habits, marking completions, and generating a heatmap for the frontend.

Endpoints:
- POST /users/:userId/habits
- GET /users/:userId/habits
- POST /users/:userId/habits/:habitId/complete
- GET /users/:userId/habits/dashboard/heatmap
